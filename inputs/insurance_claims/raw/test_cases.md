# User Test Cases

- Validate that all generated foreign keys resolve.
- Validate that claim dates do not occur before policy start dates.
- Validate that payment dates do not occur before claim dates.
- Validate that fraud flags match the fraud score threshold.
- Validate that every output table has the requested base record count for the MVP.
